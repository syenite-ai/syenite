ALCHEMY_API_KEY=your_alchemy_api_key_here
ADMIN_PASSWORD=change_this_to_a_secure_password
# On DigitalOcean App Platform, set DASHBOARD_PASSWORD at app-level instead (avoids component override)
# DASHBOARD_PASSWORD=your_dashboard_password
PORT=3000
DATABASE_PATH=./syenite.db
# Optional: seed a persistent API key on startup (survives ephemeral storage)
SEED_API_KEY=sk_your_persistent_key_here
# Li.Fi swap/bridge routing — register at portal.li.fi for fee collection
LIFI_INTEGRATOR=syenite
LIFI_FEE=0.0015

# Optional: DigitalOcean Spaces (or S3-compatible) for persistent cache so yield/rates survive redeploys
# SPACES_BUCKET=your-space-name
# SPACES_REGION=nyc3
# SPACES_KEY=your-spaces-key
# SPACES_SECRET=your-spaces-secret
